## Codebase Patterns
- 文档统一放在 `docs/` 目录下，协作模板、项目概述、开发清单等已存在
- `cloudpet-mcp/` 是独立 Node.js 子项目，有独立的 package.json 和 node_modules
- `.agents/` 和 `.cursor/` 是本地 AI 工具配置目录，已 gitignore
- `nul` 是 Windows NUL 重定向误创建的空文件，已在 .gitignore 中排除
- `test-bubble.ps1` 和 `__MACOSX/` 在当前仓库中不存在
- `package.json` 已有 `check:ts`、`check:rust`、`check`、`verify` 脚本入口

---

## 2026-04-17 12:40 - US-001
- 创建 `docs/ROOT_STRUCTURE.md`，盘点仓库所有顶层目录和关键散落文件，逐项记录用途和处理结论
- 对 `__MACOSX/`、`.agents/`、`.cursor/`、`scripts/ralph/`、`test-bubble.ps1` 逐项给出处理结论
- 明确 `cloudpet-mcp/` 定位为独立子项目、与主应用同仓维护
- 更改的文件: `docs/ROOT_STRUCTURE.md`（新增）, `scripts/ralph/prd.json`（更新 passes）
- **未来迭代的学习：**
  - `__MACOSX/` 和 `test-bubble.ps1` 在 PRD 中被提及但实际不存在于仓库
  - `package.json` 中已经有 `check:ts`、`check:rust`、`check`、`verify` 脚本（US-015 可能已部分完成）
  - `.agents/` 和 `.cursor/` 内部结构一致（都有 skills/ 子目录和 .DS_Store），可能是同一工具体系的不同配置
  - `src-tauri/` 下有 `target-codex/` 和 `target-codexv9z1Sq/` 等临时构建目录，已在 .gitignore 中排除
---

### Validation 2026-04-17 14:30 - US-001 - PASS
- Summary: 全部 5 项验收标准通过
- Evidence:
  - AC1: `docs/ROOT_STRUCTURE.md` 存在（112行），含顶层目录和顶层文件两张完整表格
  - AC2: 特定资产处理结论覆盖全部 5 项（__MACOSX/.agents/.cursor/scripts:ralph/test-bubble.ps1）
  - AC3: cloudpet-mcp 定位为「独立子项目，与主应用同仓维护」，含依据和关系说明
  - AC4: commit 963d586 仅新增 docs/ROOT_STRUCTURE.md（+111行），零删除，无源文件/构建/数据库误删
  - AC5: `npx tsc --noEmit` 零错误
---

## 2026-04-17 15:20 - US-002
- 创建 `docs/PROJECT_STATUS.md`，包含项目概述、当前目标、进行中工作、近期完成事项、下一步最小动作、主要风险阻塞、导航链接
- 内容基于当前仓库实际状态填写，包括 PRD 进度表、近期 git 提交记录、已知工程风险
- 更改的文件: `docs/PROJECT_STATUS.md`（新增）, `scripts/ralph/prd.json`（更新 passes）
- **未来迭代的学习：**
  - `docs/SESSION_LOG.md` 最后更新时间是 2026-03-25，部分内容已过时（如"正在训练定时任务需求卡"），但作为过程记录未修改
  - 项目状态文档需要与 SESSION_LOG.md 保持职责区分：PROJECT_STATUS 面向"项目事实"，SESSION_LOG 面向"协作过程"
---

### Validation 2026-04-17 22:00 - US-003 - FAIL
- Summary: US-003 完全未实现，所有 4 项 AC 均未通过
- Evidence:
  - AC1: `docs/specs/` 目录不存在（Glob 返回空）
  - AC2: 无模板或索引文档定义需求卡必含字段
  - AC3: 无导航链接指向 specs 目录（因目录不存在）
  - AC4: 跳过（前置条件未满足）
- Root Cause: Story 未被任何开发迭代拾取实现
- notes 已写入 prd.json
---

## 2026-04-17 23:10 - US-003
- 创建 `docs/specs/` 目录作为功能需求卡的统一存放位置
- 创建 `docs/specs/INDEX.md` 索引文档，定义必含字段（目标、用户路径、输入/输出、业务规则、边界异常、当前状态、验收清单）和文件命名规范
- 在 `DEVELOPMENT_GUIDE.md` 的"先看什么"列表中添加功能需求卡目录链接
- 在 `docs/PROJECT_STATUS.md` 的导航列表中添加功能需求卡目录链接
- 更改的文件: `docs/specs/INDEX.md`（新增）, `DEVELOPMENT_GUIDE.md`（更新导航）, `docs/PROJECT_STATUS.md`（更新导航）, `scripts/ralph/prd.json`（更新 passes）
- **未来迭代的学习：**
  - `docs/FEATURE_SPEC_TEMPLATE.md` 已存在且结构完整，INDEX.md 直接引用该模板而非重复定义
  - 需求卡的状态值应统一使用：`idea` / `planned` / `in-progress` / `shipped` / `paused`（后续 US-007 的 FEATURE_MATRIX 需与此保持一致）
  - 导航入口有两个：`DEVELOPMENT_GUIDE.md`（工程入口）和 `docs/PROJECT_STATUS.md`（状态入口），两者都需同步更新
---

### Validation 2026-04-17 23:30 - US-004 - FAIL
- Summary: US-004 完全未实现，所有 4 项 AC 均未通过
- Evidence:
  - AC1: `docs/specs/` 下仅有 `INDEX.md`，无 main-window / speech-bubble / settings 需求卡文件
  - AC2: 无需求卡文件存在，无法检查字段完整性
  - AC3: 无需求卡文件存在，无法检查内容风格
  - AC4: `npx tsc --noEmit` 通过，但因无交付物此项无意义
- Root Cause: Story 未被任何开发迭代拾取实现
- notes 已写入 prd.json
---

## 2026-04-17 23:50 - US-004
- 创建 `docs/specs/MAIN-WINDOW.md`、`docs/specs/SPEECH-BUBBLE.md`、`docs/specs/SETTINGS.md` 三张功能需求卡
- 每张卡包含：目标、用户路径、输入/输出、业务规则、边界与异常、当前状态、验收清单、本次不做什么
- 更新 `docs/specs/INDEX.md` 索引，补充三张需求卡链接和状态
- 更改的文件: `docs/specs/MAIN-WINDOW.md`（新增）, `docs/specs/SPEECH-BUBBLE.md`（新增）, `docs/specs/SETTINGS.md`（新增）, `docs/specs/INDEX.md`（更新索引表）
- **未来迭代的学习：**
  - 需求卡内容基于源码实际行为编写，而非 PRD 描述，确保卡片与当前实现一致
  - `speech:show` 的 duration=0 表示不自动关闭（流式传输期间），`speech:done` 才启动关闭计时
  - 设置项存储在 SQLite `settings` 表中，key-value 格式，所有值以字符串形式存储
  - 主窗口 App.tsx 是核心协调层，管理所有子窗口的互斥显示和位置同步
---

### Validation 2026-04-17 24:00 - US-004 - PASS
- Summary: 全部 4 项验收标准通过
- Evidence:
  - AC1: `docs/specs/MAIN-WINDOW.md`、`SPEECH-BUBBLE.md`、`SETTINGS.md` 均存在，INDEX.md 索引表已更新
  - AC2: 三张卡片均含目标(§2)、用户路径(§3)、输入/输出(§4)、业务规则(§5)、边界与异常(§6)、当前状态(§7)、验收清单(§9)
  - AC3: 描述行为（用户看到什么、操作链路、事件合约、数据流），无组件结构/函数实现等代码级细节
  - AC4: `npx tsc --noEmit` 零错误
  - Commit 9889b53 仅含 4 文件（+348/-1），无源文件误删
---

## 2026-04-17 24:30 - US-005
- 创建 `docs/specs/TODO.md`、`docs/specs/SCHEDULER.md`、`docs/specs/REMINDER.md` 三张功能需求卡
- 每张卡包含：目标、用户路径、输入/输出、业务规则、边界与异常、当前状态、验收清单、本次不做什么
- 明确三个模块的数据边界和触发边界：
  - Todo：通过 todos/todo_history 两张表管理待办数据，支持 AI 创建待办（轮询同步）
  - Scheduler：独立 scheduled_tasks 表，60 秒轮询，daily/interval 两种触发模式
  - Reminder：复用 todos 表的 last_reminded_at 字段，按优先级和冷却时间选择待办提醒
- 更新 `docs/specs/INDEX.md` 索引，补充三张需求卡链接和状态
- 更改的文件: `docs/specs/TODO.md`（新增）, `docs/specs/SCHEDULER.md`（新增）, `docs/specs/REMINDER.md`（新增）, `docs/specs/INDEX.md`（更新索引表）
- **未来迭代的学习：**
  - Todo/Scheduler/Reminder 三个模块共享同一数据库（zhushou.db），但各有独立的数据表
  - Reminder 不是独立窗口，而是后台服务，通过 App.tsx 集成到主窗口（表情 + 音效 + 气泡）
  - Scheduler 和 TodoPage 使用相同的子窗口位置（主窗口左侧），互斥显示
  - 这三个模块都没有专门的 Rust Tauri commands，全部通过 tauri-plugin-sql 前端直接操作 SQLite
---

### Validation 2026-04-17 25:00 - US-005 - PASS
- Summary: 全部 4 项验收标准通过
- Evidence:
  - AC1: `docs/specs/TODO.md`、`SCHEDULER.md`、`REMINDER.md` 均存在，INDEX.md 索引表已更新
  - AC2: 三张卡片均含目标(§2)、用户路径(§3)、输入/输出(§4)、业务规则(§5)、边界与异常(§6)、当前状态(§7)、验收清单(§9)
  - AC3: 数据边界和触发边界均明确：Todo（todos/todo_history 两表 + AI 轮询 1 秒 + 归档凌晨 5:00）、Scheduler（scheduled_tasks 表 + 60 秒轮询 + daily/interval 模式）、Reminder（todos.last_reminded_at + settings 表 + setTimeout 递归 + 冷却机制）
  - AC4: `npx tsc --noEmit` 零错误
---

## 2026-04-17 26:00 - US-006
- 创建 `docs/specs/FOCUS.md`、`docs/specs/AI-CHAT.md`、`docs/specs/MEMORY.md` 三张功能需求卡
- 每张卡包含：目标、用户路径、输入/输出、业务规则、边界与异常、当前状态、验收清单、本次不做什么
- 明确三个模块的行为边界：
  - Focus：番茄钟计时器，专注/休息双阶段自动切换，通过 Tauri 事件与主窗口联动，无数据持久化
  - AI Chat：流式对话（DeepSeek API）+ 屏幕感知主动发言（Gemini/GLM 视觉）+ 上下文记忆注入 + 定时任务意图识别
  - Memory：自动提取（对话后异步）+ 混合检索（向量 0.50 + 重要性 0.25 + 近期度 0.15 + 关键词 0.10）+ 去重 + 纠正 + 历史压缩 + 日摘要
- AI-CHAT.md 和 MEMORY.md 严格区分了用户可见行为（对话流式回复、个性化记忆）和实现细节（嵌入向量、评分算法）
- 更新 `docs/specs/INDEX.md` 索引，补充三张需求卡链接和状态
- 更改的文件: `docs/specs/FOCUS.md`（新增）, `docs/specs/AI-CHAT.md`（新增）, `docs/specs/MEMORY.md`（新增）, `docs/specs/INDEX.md`（更新索引表）
- **未来迭代的学习：**
  - Focus 模块是唯一没有数据库持久化的功能模块（专注数据仅在内存中）
  - AI 对话使用 OpenAI SDK 兼容接口调用 DeepSeek API，不是直接调用 Anthropic API
  - 记忆系统的嵌入向量通过 Gemini API 生成（复用 `vision_api_key`），L2 归一化后存储
  - 屏幕监控服务每 30 秒截图，通过哈希去重避免重复分析，3 分钟节流防止频繁发言
  - 三个模块共享同一数据库（zhushou.db），但各有独立的数据表
---

### Validation 2026-04-17 27:00 - US-006 - PASS
- Summary: 全部 4 项验收标准通过
- Evidence:
  - AC1: `docs/specs/FOCUS.md`、`AI-CHAT.md`、`MEMORY.md` 均存在，INDEX.md 索引表已更新
  - AC2: 三张卡片均含目标(§2)、用户路径(§3)、输入/输出(§4)、业务规则(§5)、边界与异常(§6)、当前状态(§7)、验收清单(§9)
  - AC3: AI-CHAT 和 MEMORY 严格区分用户可见行为（thinking表情/流式回复/个性化记忆/纠正响应）与实现细节（DeepSeek API/Gemini嵌入/向量评分算法/并发标志）
  - AC4: `npx tsc --noEmit` 零错误
---

## 2026-04-17 28:00 - US-007
- 创建 `docs/specs/FEATURE_MATRIX.md`，包含功能状态矩阵
- 矩阵覆盖全部 9 张需求卡：主窗口、语音气泡、AI 对话、设置、待办、提醒、定时任务、专注模式、记忆系统
- 状态值统一使用：idea / planned / in-progress / shipped / paused
- 每项功能标注优先级（P0/P1/P2）和备注说明
- 更新 `docs/specs/INDEX.md` 导航区，添加功能状态矩阵链接
- 更改的文件: `docs/specs/FEATURE_MATRIX.md`（新增）, `docs/specs/INDEX.md`（更新导航）, `scripts/ralph/prd.json`（更新 passes）
- **未来迭代的学习：**
  - 所有 9 项功能当前状态均为 shipped，矩阵可作为后续新增功能的状态追踪起点
  - 状态值与 INDEX.md 中定义的规范完全一致，后续 ROADMAP（US-008）应复用相同状态枚举
  - 优先级分级：P0（核心交互）= 主窗口 + 气泡 + AI对话；P1（任务管理）= 设置 + 待办 + 提醒；P2（增强）= 定时任务 + 专注 + 记忆
---

### Validation 2026-04-17 29:00 - US-007 - PASS
- Summary: 全部 5 项验收标准通过
- Evidence:
  - AC1: `docs/specs/FEATURE_MATRIX.md` 存在（51行）
  - AC2: 矩阵表含 #、功能名称、状态、优先级（P0/P1/P2）、需求卡链接、备注，共 9 项功能
  - AC3: 状态定义表统一使用 idea / planned / in-progress / shipped / paused，与 INDEX.md 规范一致
  - AC4: 全部 9 张需求卡（US-003~006 创建）均被矩阵引用：MAIN-WINDOW / SPEECH-BUBBLE / AI-CHAT / SETTINGS / TODO / REMINDER / SCHEDULER / FOCUS / MEMORY
  - AC5: `npx tsc --noEmit` 零错误
  - Commit c2c1c37 仅含 2 文件（+51/-0），无源文件误删
---

## 2026-04-17 30:00 - US-008
- 创建 `docs/specs/ROADMAP.md`，包含当前阶段重点（含优先级表格）、明确暂不做事项（7 项）、下一阶段候选工作（4 个方向）、阶段划分概览
- 更新 `docs/specs/INDEX.md` 导航区，添加路线图链接
- 路线图状态和优先级语言与 FEATURE_MATRIX.md 保持一致（shipped/planned + P0/P1/P2）
- 更改的文件: `docs/specs/ROADMAP.md`（新增）, `docs/specs/INDEX.md`（更新导航）, `scripts/ralph/prd.json`（更新 passes）
- **未来迭代的学习：**
  - ROADMAP.md 的阶段划分与 PRD 的 Phase 1/2/3 一一对应，后续更新路线图时需同步检查 PRD 阶段定义
  - 下一阶段候选工作已标注前置条件（如"App.tsx 治理依赖 US-009/US-012 完成"），后续选择候选方向时应先确认前置条件是否满足
  - 路线图的"暂不做"清单可指导后续 US-009~US-018 的范围控制：本轮不改产品能力、不重写代码、不搭 CI/CD
---

### Validation 2026-04-17 31:00 - US-009 - FAIL
- Summary: US-009 完全未实现，3 项实质 AC 均未通过
- Evidence:
  - AC1: `docs/architecture/ARCHITECTURE.md` 不存在（Glob 返回空）
  - AC2: 文件不存在，无法检查分层定义（UI layer / page coordination / service / Tauri-Rust / cloudpet-mcp）
  - AC3: 文件不存在，无法检查 App.tsx 治理热点标记
  - AC4: `npx tsc --noEmit` 零错误，但因无交付物此项无意义
- Root Cause: Story 未被任何开发迭代拾取实现
- notes 已写入 prd.json
---

## 2026-04-17 32:00 - US-009
- 创建 `docs/architecture/ARCHITECTURE.md`，定义 5 层架构（UI Layer / Page Coordination Layer / Service Layer / Tauri-Rust Layer / cloudpet-mcp）
- 每层包含：职责定义、边界规则（允许/禁止）、关键文件清单
- 显式标记 `src/App.tsx` 为治理热点，附新代码放置规则
- 包含跨层通信规则、依赖方向图、事件命名规范、新增 Tauri Command 步骤
- 更改的文件: `docs/architecture/ARCHITECTURE.md`（新增）, `scripts/ralph/prd.json`（更新 passes）
- **未来迭代的学习：**
  - `docs/architecture/` 目录在此 story 中新建，后续 US-010/US-011/US-012 的架构文档也放在此目录
  - AGENTS.md 已有架构与数据流章节，ARCHITECTURE.md 提供更详细的分层边界定义，两者互补
  - 数据库完全由 JS 侧 `src/services/db.ts` 管理，Rust 侧 `db.rs` 仅保留说明注释
  - App.tsx 约 900 行，是当前最大的单一文件，治理热点标记有助于后续重构决策
---

### Validation 2026-04-17 33:00 - US-010 - FAIL
- Summary: US-010 完全未实现，所有 4 项 AC 均未通过
- Evidence:
  - AC1: `docs/architecture/EVENTS.md` 不存在（Glob 返回空）
  - AC2: 文件不存在，无法检查事件文档内容
  - AC3: 文件不存在，无法验证事件名是否与代码库一致
  - AC4: `npx tsc --noEmit` 零错误，但因无交付物此项无意义
- Root Cause: Story 未被任何开发迭代拾取实现（最近 commit 为 US-009）
- notes 已写入 prd.json
---

## 2026-04-18 10:00 - US-010
- 创建 `docs/architecture/EVENTS.md`，记录 17 个事件，含事件总表（发送方/接收方/用途）、Payload 格式、通信方式说明、已知问题、新增事件检查清单
- 事件文档基于源码实际调用编写，覆盖全部 emit/emitTo/listen 调用点
- 修复文件内绝对 Windows 路径链接为纯文本引用
- 更改的文件: `docs/architecture/EVENTS.md`（新增）, `scripts/ralph/prd.json`（更新 passes）, `scripts/ralph/progress.txt`（追加进度）
- **未来迭代的学习：**
  - `docs/architecture/` 目录已有 ARCHITECTURE.md（US-009 创建），EVENTS.md 作为同级架构文档
  - FocusPage 对 focus-start/pause/reset/tick 同时使用 `emit` 和 `emitTo('main', ...)` 双重发送，属于已知遗留模式
  - 部分事件存在孤立端：`todo-mouse-enter/leave` 只有发送无监听、`speech:hide` 和 `scheduler:reload` 只有监听无发送
  - speech:* 系列事件在 App.tsx 中使用 `emit()` 而非 `emitTo('speech-bubble', ...)`，属于遗留广播模式
---

### Validation 2026-04-18 12:00 - US-010 - PASS
- Summary: 全部 4 项验收标准通过
- Evidence:
  - AC1: `docs/architecture/EVENTS.md` 存在（96行），含事件总表、Payload 格式、通信方式说明、已知问题、新增事件检查清单
  - AC2: 17 个事件均含事件名、发送方、接收方、用途描述；Payload 格式表单独列出全部事件的字段说明
  - AC3: 全部 17 个事件名与代码库 emit/emitTo/listen 调用完全匹配；CLAUDE.md 事件表中的事件均在 EVENTS.md 中有详细记录
  - AC4: `npx tsc --noEmit` 零错误
---

## 2026-04-18 14:00 - US-011
- 创建 `docs/architecture/DATA_FLOW.md`，覆盖 5 个核心模块的运行时数据流
- 每个流程标注：主要输入、处理层、持久化/副作用、可观察输出
- Todo: CRUD + 1秒轮询同步 + 跨日归档(05:00日切分点)
- Reminder: setTimeout递归调度 + 冷却=间隔 + 单轮单提醒 + 优先级排序
- Scheduler: 60秒轮询 + daily/interval双模式 + scheduler:reload孤立监听
- Chat: 流式对话(DeepSeek) + 屏幕感知主动发言(Gemini/GLM视觉) + 上下文三层注入(历史/摘要/记忆)
- Memory: 实时提取 + 历史压缩 + 混合检索打分(向量0.50+重要性0.25+近期度0.15+关键词0.10) + 三阶段去重 + 纠正
- 包含跨模块数据流总览图和关键约束说明
- 更改的文件: `docs/architecture/DATA_FLOW.md`（新增）, `scripts/ralph/prd.json`（更新 passes）
- **未来迭代的学习：**
  - Reminder 的调度间隔与冷却阈值耦合为同一数值，无法独立配置
  - AI 不直接创建 Todo，而是通过 extractScheduleIntentAsync 创建 scheduled_tasks
  - Chat 的记忆注入分三层：短期(20条对话历史)、中期(2天日摘要)、长期(混合检索15条记忆)
  - 屏幕习惯记忆每10分钟写入一次，importance=2，与对话记忆共存于 user_memories 表
  - 所有数据库操作由 JS 侧 tauri-plugin-sql 完成，Rust 侧不直接操作数据库
---

### Validation 2026-04-18 15:00 - US-011 - PASS
- Summary: 全部 4 项验收标准通过
- Evidence:
  - AC1: `docs/architecture/DATA_FLOW.md` 存在（308行），含 5 大模块 + 跨模块总览图
  - AC2: 覆盖 Todo（4子流程）、Reminder（1子流程+设计要点）、Scheduler（3子流程）、Chat（2子流程）、Memory（5子流程）
  - AC3: 每个子流程均以「输入→处理→持久化→输出」四段式结构描述，包含关键文件表
  - AC4: `npx tsc --noEmit` 零错误
---

## 2026-04-18 16:00 - US-012
- 创建 `docs/architecture/TECH_DEBT.md`，记录 8 条技术债，覆盖严重程度分级（高/中/低）
- 5 条必记问题全部覆盖：App.tsx 过重(TD-001)、轮询偏多(TD-002)、事件边界松散(TD-003)、状态真相可能重复(TD-004)、人工验证成本高(TD-005)
- 额外记录 3 条从架构和数据流分析中发现的问题：Reminder 调度间隔与冷却耦合(TD-006)、数据库操作全在 JS 侧(TD-007)、AI 对话上下文组装集中度不足(TD-008)
- 每条技术债包含：问题现象、当前影响、建议处理方向、是否阻塞后续开发
- 包含严重程度分布表和治理优先级建议（短期/中期/长期）
- 更改的文件: `docs/architecture/TECH_DEBT.md`（新增）, `scripts/ralph/prd.json`（更新 passes）
- **未来迭代的学习：**
  - 技术债文档与 ARCHITECTURE.md、DATA_FLOW.md、EVENTS.md 三份文档互补：架构定义"应该怎样"，技术债记录"哪里不符合"
  - PRD 要求的 5 条必记问题中，App.tsx 过重是唯一标记为"高严重程度"且"阻塞后续开发"的项
  - 事件边界松散（TD-003）与 EVENTS.md 的"已知问题"一节有重叠，后续修复事件时应同步更新两份文档
  - 严重程度定义表应在后续持续维护，新增技术债时需对照此表分级
---

### Validation 2026-04-18 16:30 - US-012 - PASS
- Summary: 全部 4 项验收标准通过
- Evidence:
  - AC1: `docs/architecture/TECH_DEBT.md` 存在（128行），含 8 条技术债、严重程度定义表、治理优先级建议
  - AC2: 5 条必记问题全部覆盖：App.tsx 过重(TD-001)、轮询偏多(TD-002)、事件边界松散(TD-003)、状态真相可能重复(TD-004)、人工验证成本高(TD-005)
  - AC3: 每条技术债均含问题现象、当前影响、建议方向、是否阻塞后续开发四字段，格式统一
  - AC4: `npx tsc --noEmit` 零错误
  - Commit 67b4073 仅含 3 文件（+179/-1），无源文件误删
---

## 2026-04-18 17:00 - US-013
- 验证 `docs/process/TEST_STRATEGY.md` 已存在且内容完整（由前次迭代 commit ecf38dc 创建）
- 文档区分三层验证：L1 自动化静态检查（TypeScript 编译 + Rust 编译）、L2 人工验收检查（桌面交互/视觉/多窗口）、L3 故意不覆盖（多显示器/DPI/网络异常等）
- 明确列出多窗口行为、透明度、鼠标穿透、系统托盘、窗口位置联动为必须人工验证的场景
- 更改的文件: `scripts/ralph/prd.json`（更新 US-013 passes 为 true）
- **未来迭代的学习：**
  - `docs/process/` 目录在前次迭代中已创建，后续 US-014 的 REGRESSION_CHECKLIST.md 也放在此目录
  - TEST_STRATEGY.md 引用了 REGRESSION_CHECKLIST.md（US-014 创建），当前该文件尚不存在
  - 项目当前没有 test 脚本（package.json 中无 test 命令），自动化检查仅覆盖编译期
  - L2 人工验收的触发条件明确列出了 5 种代码改动场景，可作为 code review 检查清单
---

## 2026-04-18 18:00 - US-014
- 创建 `docs/process/REGRESSION_CHECKLIST.md`，包含 6 大模块共 19 项可观测验证操作
- 清单覆盖：主窗口显示（3 项）、子窗口打开（6 项）、待办基本操作（4 项）、定时任务刷新（3 项）、专注状态变化（4 项）、语音气泡显示（3 项）
- 每项验证操作格式统一：「验证操作」+「通过标准」，描述具体可观测行为而非模糊声明
- 包含结果记录表和使用方式说明
- 更改的文件: `docs/process/REGRESSION_CHECKLIST.md`（新增）, `scripts/ralph/prd.json`（更新 passes）
- **未来迭代的学习：**
  - `docs/process/` 目录在 US-013 中创建，REGRESSION_CHECKLIST.md 与 TEST_STRATEGY.md 同目录
  - TEST_STRATEGY.md 第 112 行已引用 `docs/process/REGRESSION_CHECKLIST.md`，创建后链接即生效
  - 定时任务刷新验证（4.2）需要等待 1 分钟，是清单中最耗时的单项
  - 回归清单基于需求卡（US-004/US-005/US-006）中记录的实际用户路径编写
---

## 2026-04-18 20:00 - US-015
- 修复 `package.json` 中 `check:ts` 命令，从 `tsc --noEmit` 更新为 `npx tsc --noEmit`（匹配 AC 要求）
- 在 `docs/process/TEST_STRATEGY.md` 中新增 `verify` 职责说明：统一验证入口，执行全量自动检查后提示进入人工回归
- 四个检查脚本（check:ts / check:rust / check / verify）均已就位且职责清晰
- 更改的文件: `package.json`（修复 check:ts 命令）, `docs/process/TEST_STRATEGY.md`（新增 verify 职责说明）, `scripts/ralph/prd.json`（更新 passes）
- **未来迭代的学习：**
  - `package.json` 的检查脚本在 US-015 之前已基本存在（progress.txt Codebase Patterns 已记录），本次仅需微调命令格式和补文档
  - `verify` 当前实现为 `npm run check`，与 AC 要求的"执行自动检查并提示需要人工回归"之间通过文档桥接，而非脚本本身输出提示
  - TEST_STRATEGY.md 是记录检查命令职责的最佳位置，因为它是验证边界的单一事实来源
---
